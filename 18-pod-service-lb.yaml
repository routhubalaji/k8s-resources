apiVersion: v1
kind: Pod
metadata:
  name: service-lb-demo
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: load-balance
spec:    
  containers:
  - name: nginx
    image: nginx
